{
  "name": "tacticalblocks-workspace",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "dev": "concurrently \"npm --prefix server run dev\" \"npm --prefix client run dev\"",
    "verify": "npm --prefix client run typecheck && npm --prefix server run typecheck"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
